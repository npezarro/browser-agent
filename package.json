{
  "name": "browser-agent",
  "version": "1.0.0",
  "description": "Generic browser agent relay — lets Claude CLI control a live browser via Tampermonkey",
  "main": "agent-server.js",
  "scripts": {
    "start": "node agent-server.js",
    "deploy": "bash deploy.sh",
    "test": "node --test test/*.test.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.6.0",
    "globals": "^17.7.0"
  }
}
